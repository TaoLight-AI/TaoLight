// Runtime config for TaoLight mini program.
// Keep secrets on backend/cloud functions only.
module.exports = {
  // Set to your deployed backend URL when ready, e.g. https://api.example.com
  apiBaseUrl: '',
  // true = use local mock fallback when backend is unavailable.
  allowMockFallback: true,
  requestTimeout: 30000
}
